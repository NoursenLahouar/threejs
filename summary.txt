PHASE 1 SUMMARY: INITIALIZING THE FOUNDATION
--------------------------------------------------

1. PROJECT SETUP
   - Initialized a Next.js 14+ project with TypeScript and Tailwind CSS.
   - Configured `tsconfig.json` for proper module resolution ("bundler").
   - Set up `globals.css` with a dark, premium aesthetic.

2. CORE ARCHITECTURE (Three.js + React Context)
   - Created `src/context/ThreeContext.tsx` & `src/components/SceneProvider.tsx`.
   - ACTS AS THE "ENGINE": Handles Scene, Camera, Renderer, Loop, Resize, Cleanup.

3. VIEWPORT & CONTROLS
   - Created `src/components/ViewPort.tsx`.
   - Created `src/components/Three/OrbitControls.tsx` with Context integration.

PHASE 2 SUMMARY: CORE EDITOR FEATURES
--------------------------------------------------
1. EDITOR INFRASTRUCTURE
   - Created `src/context/EditorContext.tsx`: Manages objects state.
   - Actions: `addObject`, `selectObject`, `updateObject`.

2. SCENE SYNCHRONIZATION
   - Created `src/components/Three/ObjectManager.tsx`: Syncs React State -> Scene.
   - Supports Primitives and GLTF (Mushroom).

3. INTERACTION TOOLS
   - Created `src/components/Three/SelectionManager.tsx`: Recursive Raycasting.
   - Created `src/components/Three/TransformControlsWrapper.tsx`: Transformation.
   - Created `src/components/Three/SelectionHighlight.tsx`: Selection Box.

PHASE 3 SUMMARY: PROPERTIES PANEL & LAYOUT
--------------------------------------------------
1. UI COMPONENTS
   - Created `src/components/UI/PropertiesPanel.tsx`.
   - Implemented 2-way data binding for Position, Rotation, Scale, Color.
   
2. LAYOUT
   - Updated `src/app/page.tsx` for Split-View Layout.

PHASE 4 SUMMARY: ACTIONS [Current Branch: feat/transform-modes]
--------------------------------------------------
1. DELETE & DUPLICATE
   - Implemented Delete/Duplicate logic in Context & UI.
   - Added Keyboard Shortcuts.

2. TRANSFORM MODES
   - Created `TransformToolbar.tsx` with Move, Rotate, Scale buttons.
   - Updated `EditorContext.tsx` to handle `transformMode`.
   - REFACTORED `TransformControlsWrapper.tsx` to ensure robust mode switching and fix "missing axes" bug.

3. UI REFINEMENT [Current Branch: feat/add-mesh-dropdown]
   - **Add Mesh Dropdown**: Consolidated "Add Object" buttons into a single dropdown menu (`AddMeshDropdown.tsx`).
   - Order: Mushroom -> Box -> Sphere. Using Tailwind for styling.
   - **Scene Outliner Sidebar** [Current Branch: feat/scene-outliner]:
     - Created `SceneOutliner.tsx` to list all scene objects.
     - Implemented `RightSidebar.tsx` to stack Outliner (top) and Properties (bottom).
     - Clicking an item in the list selects it and shows its properties.
